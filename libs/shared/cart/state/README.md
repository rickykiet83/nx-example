# shared-cart-state

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-cart-state` to build the library.

## Running unit tests

Run `nx test shared-cart-state` to execute the unit tests via [Jest](https://jestjs.io).
