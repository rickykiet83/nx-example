# shared-product-state

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-product-state` to build the library.

## Running unit tests

Run `nx test shared-product-state` to execute the unit tests via [Jest](https://jestjs.io).
