# shared-product-ui

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-product-ui` to build the library.

## Running unit tests

Run `nx test shared-product-ui` to execute the unit tests via [Jest](https://jestjs.io).
